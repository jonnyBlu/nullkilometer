<%= simple_form_for(PointOfSale.new) do |f| %>

  <div class="form-inputs">
	  <%= f.input :name %>
	  <%= f.input :address %>
	  <%= f.input :product_category_ids%>
	  <%= f.input :description, :as => :text,  :input_html => { :rows => "10" } %>
  </div>

  <%= f.button :submit %>

<% end %>
